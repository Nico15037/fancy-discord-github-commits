name: Discord Webhook Workflow

on:
  push:
    branches: [ main ]

jobs:
  test-webhook:
    runs-on: ubuntu-latest

    steps:
      - name: Discord notification
        uses: Ilshidur/action-discord@master
        env:
          DISCORD_WEBHOOK: ${{ secrets.DISCORD_WEBHOOK }}
          DISCORD_USERNAME: "once I get up there"
          DISCORD_AVATAR: "https://www.farawaymoon.com/wp-content/uploads/sideshow-logo.png"
        with:
          args: "you will fuck it up"
